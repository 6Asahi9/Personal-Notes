#include <iostream>
// use ctrl + alt + n to run the code in terminal
int main(){
  // std means standard and cout means character out and all together  means standard character output, << means output
  std::cout << "i like pizza" << std::endl; //?std endl means end line so next thing will start from new line
  std::cout << "its really good" << '\n'; //?this is a new line character ,
  // it does the same thing but faster but end line can flush the output buffer  
  return 0;
}
//need a complier and terminal too